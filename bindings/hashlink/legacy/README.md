# HashLink legacy surface

API v3 treats old raw dynamic launch, descriptor V2 metadata, field mirror fallback, and packed compound workarounds as migration-only surfaces. They are not imported by the v3 docs or smoke tests.
